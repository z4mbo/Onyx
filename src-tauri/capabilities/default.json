{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Core access for the Onyx app, dictation HUD and research overlay.",
  "windows": ["main", "hud", "agent"],
  "permissions": ["core:default"]
}
